{% extends 'TemploBundle::base.html.twig' %}


{% block head_style %}
 {{ parent() }}
<link href="{{ asset('assets/bootstrap-wizard/bootstrap-wizard.css') }}" rel="stylesheet">

{% endblock %}

{% block foot_script_assetic %}
 {{ parent() }}

<script src="{{ asset('js/bootstrap-fileupload.js') }}"></script>

{% javascripts   
    '@TemploBundle/Resources/public/js/publish/publish-common.js'
   
    %}
<script src="{{ asset_url }}"></script>
{% endjavascripts %}


<script type="text/javascript">
// Initialize the widget when the DOM is ready
$(function() {
            
    var image_count=0;
    
    $('#addimage').click(function(e){
       // var table = ;
        var tr = document.createElement('tr');
        var td1 = document.createElement('td');
        var td2 = document.createElement('td');
        var td3 = document.createElement('td');
        
        input = document.createElement('input');
        input.setAttribute('name','image'+image_count);
        input.setAttribute('type','file')
        td1.appendChild(input);
        
        tr.appendChild(td1);
        tr.appendChild(td2);
        tr.appendChild(td3);
        
        $('#image-list tbody').append(tr);
        image_count++;
    });

});
</script>

{% endblock  %}



{% block main %}   

<div class="container">   
    <!-- The file upload form used as target for the file upload widget -->
    <form id="fileupload" action="{{ path('upload-test') }}" method="POST" enctype="multipart/form-data">      
        <br />
        <table class="table table-condensed" id="image-list">
        <thead>
          <tr>   
            <th>First Name</th> 
            <th>First Name</th>        
            <th><button id="addimage" class="btn btn-primary" type="button">Subsdmit</button></th>
          </tr>
        </thead>
        <tbody>         
        </tbody>
      </table>
      <input type="submit" value="Submit" />
    </form>
    
   
</div>


{% endblock %}
